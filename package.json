{
  "name": "kaidan-archive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "lint": "astro check && node scripts/link-check.mjs",
    "test": "vitest run",
    "content:check": "node scripts/content-check.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.3.1",
    "astro": "^5.13.5",
    "pagefind": "^1.3.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
